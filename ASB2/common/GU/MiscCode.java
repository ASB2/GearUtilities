package GU;

public class MiscCode {

    /*
     * 
     * 
     * GL11.glPushMatrix();
     * 
     * RenderItem renderitem =
     * (RenderItem)RenderManager.instance.getEntityClassRenderObject
     * (EntityItem.class); EntityItem entityitem = new EntityItem(tile.worldObj,
     * x, y, z, new ItemStack(Item.bread)); entityitem.age = 0;
     * entityitem.hoverStart = 2.0F;
     * 
     * GL11.glTranslatef((float)x + .5F, (float)y + .5F, (float)z + 0.5F);
     * 
     * renderitem.doRenderItem(entityitem, 0.0D, 0.0D, 0.0D, 0.0F, 0.0F);
     * GL11.glPopMatrix();
     */
}
